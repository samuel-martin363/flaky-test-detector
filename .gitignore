# dependencies (there are none right now, but just in case)
node_modules/

# the tool's own local output — this is per-machine, not source code
.flaky-detector-data/

# demo artifact regenerated by `npm run demo`
results.xml

# private business notes — keep local, never publish
STRATEGY.md
TIERS.md

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
